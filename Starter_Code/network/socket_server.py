import socket
import threading
import time

server_socket = None  # add at top of socket_server.py

def start_socket_server(self_ip, port, handler):
    # TODO: Create a TCP socket and bind it to the peer’s IP address and port. Start listening on the socket for receiving incoming messages.
    pass