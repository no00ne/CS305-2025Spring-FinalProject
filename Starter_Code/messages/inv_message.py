from utils import generate_message_id

def create_inv(sender_id, block_ids):
    # TODO: Create the JSON format of an “INV” message, which should include: {message type, sender’s ID, related blocks’ ID, and message ID}.
    pass
